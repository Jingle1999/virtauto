<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>virtauto – Web Consistency Report</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/styles.css" />
  <style>
    body{max-width:980px;margin:2rem auto;padding:0 1rem;line-height:1.6}
    .status{font-weight:700}
    .ok{color:#0a7f3f}.fail{color:#b00020}
    pre{background:#0f172a;color:#e2e8f0;padding:1rem;border-radius:.5rem;overflow:auto}
  </style>
</head>
<body>
  <h1>Web Agents Consistency Report</h1>
  <p class="status {{ 'ok' if ok else 'fail' }}">
    Status: {{ 'PASSED' if ok else 'FAILED' }}
  </p>
  <p>Generated: {{ generated }}</p>
  {% if ok %}
    <p>All checks passed.</p>
  {% else %}
    <h2>Details</h2>
    <ul>
      {% for item in items %}
        <li>{{ item }}</li>
      {% endfor %}
    </ul>
  {% endif %}
  <hr/>
  <p><a href="/index.html">← Back to home</a></p>
</body>
</html>
