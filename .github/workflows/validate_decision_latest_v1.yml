name: validate_decision_latest_v1

on:
  pull_request:
    paths:
      - "ops/decisions/latest.json"
      - "ops/contracts/schemas/decision_latest_v1.schema.json"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: governance-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate_decision_latest_v1:
    name: validate_decision_latest_v1
    uses: ./.github/workflows/_governance_template.yml
    with:
      mode: "ajv"
      job_display_name: "validate_decision_latest_v1"
      runs_on: "ubuntu-24.04"
      node_version: "20.11.1"
      schema_path: "ops/contracts/schemas/decision_latest_v1.schema.json"
      data_path: "ops/decisions/latest.json"
      timeout_minutes: 10
