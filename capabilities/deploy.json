{
  "capability": "deploy",
  "description": "Deploy the website to production via GitHub Pages with deterministic health check and rollback.",
  "critical": true,

  "agents": {
    "primary": "deploy_agent_v1",
    "secondary": "deploy_agent_v1_backup"
  },

  "routing": {
    "strategy": "deterministic_failover",
    "failover_conditions": [
      {
        "condition": "healthcheck_failed",
        "action": "route_to_secondary"
      },
      {
        "condition": "execution_failed",
        "scope": "primary_deploy",
        "action": "route_to_secondary"
      }
    ]
  },

  "constraints": {
    "learning_allowed": false,
    "optimization_allowed": false,
    "autonomy_increase_allowed": false
  },

  "traceability": {
    "trace_required": true,
    "trace_artifact": "ops/reports/deploy_trace.jsonl"
  }
}