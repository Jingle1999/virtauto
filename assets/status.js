(async()=>{const root=document.getElementById('status-app');if(!root)return;try{const res=await fetch('news.json',{cache:'no-store'});const items=await res.json();root.innerHTML=items.map(x=>`<div class='card'><div class='kicker'>${x.timestamp} • ${x.agent||'CI/CD'}</div><h3>${x.summary}</h3></div>`).join('');}catch(e){root.innerHTML='<p class="muted">No data yet…</p>';}})();