# memory/embeddings/

This folder will store vector embeddings for documents in `../documents/`.

Each embedding file will be JSON with:
- `document_id`
- `embedding_model`
- `vector` (or reference to external store)
- `created_at`
